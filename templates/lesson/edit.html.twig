{% extends 'base.html.twig' %}

{% block title %}{{ lesson.LessonName }}{% endblock %}

{% block body %}
    <h1>{{ lesson.LessonName }}</h1>

    {{ include('lesson/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_lesson_show', {'id': lesson.id}) }}">Вернуться к уроку</a>

    {{ include('lesson/_delete_form.html.twig') }}
{% endblock %}
