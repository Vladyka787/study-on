<nav class="navbar navbar-expand-lg bg-light">
    <div class="container-fluid"><a class="navbar-brand" href="{{ path('app_course_index') }}">Study-ON</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
                aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation"><span
                    class="navbar-toggler-icon"></span></button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                {% if is_granted("IS_AUTHENTICATED_FULLY") %}
                    <li class="nav-item"><a class="nav-link" href="{{ path('app_profile') }}">Профиль</a></li>
                    <li class="nav-item"><a class="nav-link" href="{{ path('app_logout') }}">Выйти</a></li>
                {% endif %}
                {% if is_granted("IS_ANONYMOUS") %}
                    <li class="nav-item"><a class="nav-link" href="{{ path('app_login') }}">Войти</a></li>
                    <li class="nav-item"><a class="nav-link" href="{{ path('app_register') }}">Регистрация</a></li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>



